After any deploy to live (config/DB/cloud) the deploying window MUST re‑pull the live object and assert the specific change is present (e.g. "104 tools now have failure messages", "lookup p95 <2s under concurrency") BEFORE claiming done. "I edited/PATCHed it" ≠ done. "I re‑read it live and the change is there" = done. This rule applies to rules 140, 141, 146 (frankenstein routing facts).